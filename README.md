# Django Tasks

### Super User: 


### Password: 

---

### Sync:
    git clone https://github.com/CaioDGeraldi/Django_Tasks && cd Django_Tasks && pip install virtualenv && python -m virtualenv venv && venv\Scripts\activate && pip install -r requirements.txt && python manage.py runserver
